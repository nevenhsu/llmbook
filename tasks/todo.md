# Tasks

## Active

- [x] Tighten Persona v2 main static prompt blocks so every flow/contentMode has enough AI task context.
- [x] Reduce Persona v2 quality audit prompts to at most two aspects per flow/contentMode.
- [x] Clarify Phase 2.5 static prompt constants so audit and quality repair are split by flow/contentMode.
- [x] Expand Phase 2.5 main prompt examples to cover all flow/contentMode combinations.
- [x] Clarify Phase 2.7 that missing fields are repairable and persona generation uses the shared schema gate.
- [x] Update Phase 2.7 so shared schema repair is a bounded loop from finish repair into field patch when needed.
- [x] Correct Phase 2.7 so schema repair is shared for every LLM JSON output, with Persona v2 as first integration.
- [x] Create Phase 2.7 plan for schema repair before quality audit.
- [x] Ground Phase 2.7 in the Phase 3 shared JSON finish/field-patch repair framework.
- [x] Correct all Persona v2 audit examples so audit checks output quality only, not schema.
- [x] Correct the DeepSeek prompt handoff plan so task, audit, and output-contract blocks are static constants.
- [x] Update schema repair guidance to reference the Phase 3 shared JSON repair framework instead of standalone templates.
- [x] Record the prompt static/dynamic boundary correction in `tasks/lessons.md`.
- [x] Create a DeepSeek handoff plan for Persona Core v2 prompt examples and dynamic-block labeling.
- [x] Ensure the handoff plan warns against copying example text or policy examples into production prompt constants.
- [x] Verify the plan covers all Phase 2 prompt stages and records unclear-rule escalation to the user.
- [x] Create a Phase 3 plan for simplifying one-stage Persona Core v2 generation and shared JSON finish/field-patch repair.
- [x] Verify the new plan covers the requested prompt blocks, reference rules, `persona_fit_probability`, deleted `do_not_imitate`, and no implementation edits.
- [x] Refine the repair plan so `finishReason=length` asks the repair model to finish the previous output with full schema context before field-patch fallback.
- [x] Expand `finishReason=length` handling into a reusable response-finishing framework with continuation state, prompt contract, salvage rules, and debug evidence.

## Current References

- LLM-flow docs target folder: `docs/ai-agent/llm-flows`
- LLM JSON contract: `docs/dev-guidelines/08-llm-json-stage-contract.md`

## Review

- 2026-05-07: Reviewed the Phase 2.5 main static blocks for `post_plan`, `post_body`, `comment`, and `reply`. Added exact static action/content/task text for all 8 flow/contentMode combinations, tightened ambiguous story wording, and made stage boundaries explicit so DeepSeek can hardcode clear static constants without embedding dynamic persona, board, post, or comment context. No runtime code, schema, or production prompt files were changed.
- 2026-05-07: Tightened the Phase 2.5 DeepSeek handoff so every quality audit context and audit output contract has at most two checks: one flow/content-quality key plus `persona_fit`. Story, board, novelty, markdown, thread, policy, and procedure concerns are now folded into those two aspects, and quality repair prompts only repair the matching failed audit aspects. Added matching checklist/test expectations and a durable lesson. No runtime code, schema, or production prompt files were changed.
- 2026-05-07: Tightened `plans/persona-v2/2026-05-07-persona-core-v2-prompt-examples-deepseek-handoff-plan.md` so DeepSeek has an explicit static constant inventory for main generation, audit, and quality repair by flow/contentMode. Audit output contracts are now split into discussion/story variants instead of using `narrative_fit: not_applicable`, and quality repair contexts are explicitly content-mode-specific while still reusing the corresponding main output contract. Added matching tests/checklist items and a durable lesson. No runtime code, schema, or production prompt files were changed.
- 2026-05-07: Expanded `plans/persona-v2/2026-05-07-persona-core-v2-prompt-examples-deepseek-handoff-plan.md` so main prompt examples now clearly cover all 8 flow/contentMode combinations: `post_plan`, `post_body`, `comment`, and `reply` for both `discussion` and `story`. Renumbered later schema repair, audit, and quality repair examples so the plan now has 20 labeled examples total. No runtime code, schema, or production prompt files were changed.
- 2026-05-07: Clarified `plans/2026-05-07-persona-core-v2-schema-repair-before-audit-plan.md` so missing fields are first-class field-patch repair targets when allowlisted, including nested Persona Core v2 generation fields. The plan now explicitly requires one-stage Persona Core v2 generation output to use the same shared schema gate before preview/save/semantic checks, with Persona-specific schema knowledge supplied by an adapter rather than the shared repair module. Added matching tests/checklist items and a durable lesson. No runtime code, schema, or production prompt files were changed.
- 2026-05-07: Refined `plans/2026-05-07-persona-core-v2-schema-repair-before-audit-plan.md` so shared JSON schema repair is a bounded loop instead of one-shot branching. Finish repair now returns to parse/schema validation and can fall through to field-patch repair when it produces parseable JSON with missing or invalid fields; field patch may also retry within bounded limits. Added matching tests/checklist items and a durable lesson. No runtime code, schema, or production prompt files were changed.
- 2026-05-07: Corrected `plans/2026-05-07-persona-core-v2-schema-repair-before-audit-plan.md` after user feedback so schema repair is a shared framework for every LLM JSON output, not Persona-only. The plan now names a generic `runSharedJsonSchemaGate`, moves core files under `src/lib/ai/json-repair`, treats Persona v2 as a thin adapter/first integration target, adds migration coverage for other JSON-producing flows, and records that all app-owned JSON consumers should receive schema-gated typed objects. Added a durable lesson. No runtime code, schema, or production prompt files were changed.
- 2026-05-07: Created `plans/2026-05-07-persona-core-v2-schema-repair-before-audit-plan.md` as Phase 2.7 of Persona Core v2. The plan defines schema repair as a mandatory gate between main generation and quality audit, references the Phase 3 shared JSON finish/field-patch repair framework, separates parse/schema repair from quality audit, defines flow-specific schemas and repair paths, lays out orchestration and debug metadata, and adds DeepSeek implementation/testing tasks. No runtime code, schema, or production prompt files were changed.
- 2026-05-07: Corrected Persona v2 audit guidance in `plans/persona-v2/2026-05-07-persona-core-v2-prompt-examples-deepseek-handoff-plan.md` after user feedback. Audits now explicitly assume schema parsing and schema repair already passed; audit check standards are quality-only; `candidate_count` was removed in favor of quality checks; audit examples now forbid checking generated-output schema, required keys, field types, parseability, candidate count, and metadata shape. Added a durable lesson for the audit boundary. No runtime code, schema, or production prompt files were changed.
- 2026-05-07: Corrected `plans/persona-v2/2026-05-07-persona-core-v2-prompt-examples-deepseek-handoff-plan.md` after user feedback. The plan now treats `task_context`, `audit_context`, and `output_contract` as static prompt constants; rewrites output contracts in key/type JSON form; marks post body/comment/reply generated content as markdown strings; points schema repair to the Phase 3 shared JSON finish/field-patch repair framework; makes audit context check standards explicit; and states quality repair reuses the matching main flow output-contract constant. Added matching durable lessons. No runtime code, schema, or production prompt files were changed.
- 2026-05-07: Created `plans/persona-v2/2026-05-07-persona-core-v2-prompt-examples-deepseek-handoff-plan.md` as Phase 2.5 of Persona Core v2. The plan gives DeepSeek labeled prompt examples across `post_plan`, `post_body`, `comment`, and `reply` for main, schema repair, audit, and quality repair; labels dynamic block ownership; warns that examples are docs/test fixtures only; adds hardcoding guardrails and regression-test tasks; and requires asking the user before encoding unclear prompt rules. No runtime code, schema, or production prompt files were changed.
- 2026-05-07: Created `plans/persona-v2/2026-05-07-one-stage-persona-generation-prompt-simplification-plan.md` as Phase 3 of Persona Core v2. The plan defines the exact seven-block one-stage generation prompt, moves reference resolution into `reference_style.reference_names` and `reference_style.other_references`, adds `persona_fit_probability`, deletes `reference_style.do_not_imitate` from v2 data, preserves non-imitation as prompt/validation behavior, and specifies JSON finish repair for `finishReason=length` plus field-patch repair fallback across LLM audit/repair flows. No runtime code, schema, or prompt implementation files were changed.
- 2026-05-07: Refined the Phase 3 JSON repair plan so `finishReason=length` with invalid or still-unparseable JSON first uses a schema-grounded finish-output repair: provide the full schema, validation rules, previous output context, likely open field path, and remaining fields; ask for only the missing continuation; concatenate, parse, and validate before falling back to field patches. No implementation files were changed.
- 2026-05-07: Expanded the Phase 3 length-repair design into a reusable response-finishing framework. The plan now defines finish attempt state, truncation classes, deterministic tail closure, append-only continuation prompts, prefix-preservation checks, salvage handling for repeated-prefix or whole-object repair responses, bounded retry rules, debug metadata, and field-patch fallback. No implementation files were changed.
- 2026-05-07: Integrated `plans/persona-v2/output-metadata-probability.md` into the Phase 2 Persona Core v2 prompt family integration plan. The plan now carries `metadata: { probability: number }` through post body, full post, comment, and reply output contracts; documents parser defaulting to `0`; keeps `post_plan` unchanged; preserves metadata through schema/quality repair contracts; explicitly excludes probability-specific audit gates, DB columns, and UI from this phase; and adds migration/test expectations for parser and flow propagation. No runtime code was changed.
- 2026-05-06: Updated Phase 1 and Phase 2 Persona Core v2 plans with compact persona-specific thinking procedures. Phase 1 now adds `PersonaThinkingProcedure` under `mind`, runtime packet procedure rendering, validation, derivation, flow-specific discussion/story procedure rules, audit procedure-fit targets, and differentiation tests. Phase 2 now places the procedure inside `persona_runtime_packet`, preserves final-output-only behavior, adds procedure non-exposure rules, adds `procedure_fit` audit coverage, and documents three same-context persona examples. No code, schema, or prompt implementation files were changed.
- 2026-05-06: Created `plans/persona-v2/2026-05-06-persona-core-v2-prompt-family-integration-plan.md` as Phase 2 of Persona Core v2. The plan maps current prompt blocks to keep/replace/merge/remove decisions, defines the new block order around `PersonaRuntimePacket.renderedText`, covers discussion and story mode for post/comment/reply flows, preserves audit/schema-repair/quality-repair behavior, proposes files and function signatures, lays out the migration sequence, and includes risk analysis plus focused tests. No code, schema, or prompt implementation files were changed.
- 2026-05-06: Updated `plans/persona-v2/2026-05-06-persona-core-v2-runtime-projection-plan.md` with compact narrative support for story content. The update uses flexible short string narrative fields, adds `ContentMode = "discussion" | "story"`, content-mode-specific runtime packet rules for post planning, post bodies, comments, replies, and audit, strict compactness validation, v1 narrative mapping, required generation prompt changes, story-mode implementation phases, and three-persona same-prompt differentiation tests. No code, schema, or prompt implementation files were changed.
