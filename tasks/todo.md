# Tasks

## Active

- [x] Fix the client/server import boundary so `AiAgentLabPage` no longer pulls `sharp` / `child_process` into the browser build.
- [x] Add a regression that prevents client components from importing the root ai-agent server barrel again.
- [x] Re-run targeted tests, `tsc`, and the repo build command before closing the bug.

## Review

- Fixed the `child_process` / `detect-libc` browser-build failure by removing `AiAgentLabPage`'s import of the root `@/lib/ai/agent` barrel, adding a client-safe [client.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/client.ts) export surface for preview builders, and keeping `AiAgentRuntimeSourceSnapshot` on its server-safe module path.
- Added [AiAgentLabPage.imports.test.ts](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentLabPage.imports.test.ts) as a regression so client components do not drift back to the root ai-agent server barrel.
- Marked [media-job-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/execution/media-job-service.ts) as `server-only`, so future accidental client imports of the Sharp-backed media pipeline fail at the boundary instead of leaking Node-only dependencies into browser bundles.
- Verified the fix with `npm test -- src/components/admin/agent-panel/AiAgentLabPage.imports.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts`, `npm exec tsc -- --noEmit --pretty false --incremental false`, and `npm run build`.
- Expanded the AI agent panel spec into an operator + dev-lab subplan that covers selector inputs/outputs, persona-group controls, candidate/task previews, execution previews, modal-based runtime inspection, and a full UI test flow.
- Added a dedicated memory UI test subplan for focused memory write/compression validation.
- Added [AI_AGENT_INTEGRATION_DEV_PLAN.md](/Users/neven/Documents/projects/llmbook/plans/ai-agent/AI_AGENT_INTEGRATION_DEV_PLAN.md) as the canonical dev-plan entry, with phase sequencing, PM gates, Dev checklists, and workstream/subplan ownership.
- Consolidated active ai-agent plans under `/plans/ai-agent/sub/` with stable, non-dated filenames and updated repo docs to treat the integration plan as the single entry point.
- Added a progress-tracking section and phase status board to [AI_AGENT_INTEGRATION_DEV_PLAN.md](/Users/neven/Documents/projects/llmbook/plans/ai-agent/AI_AGENT_INTEGRATION_DEV_PLAN.md) so PM and Dev can track status from one canonical table.
- Updated the persona runtime planning contract so selector grouping uses `ai_agent_config.selector_reference_batch_size` instead of a hardcoded `100`.
- Updated the memory compressor contract so it re-checks orchestrator readiness before each next single-persona compression job and yields immediately when the next orchestrator cycle is due.
- Added [20260329103000_ai_agent_config_contract_alignment.sql](/Users/neven/Documents/projects/llmbook/supabase/migrations/20260329103000_ai_agent_config_contract_alignment.sql) to align `ai_agent_config` with the current runtime contract, including `selector_reference_batch_size`.
- Updated [schema.sql](/Users/neven/Documents/projects/llmbook/supabase/schema.sql) and [verification.sql](/Users/neven/Documents/projects/llmbook/supabase/verification.sql) so fresh DB setup and verification expect the same `ai_agent_config` key set.
- Readiness check result: implementation can start now, but as a Phase 1 foundation slice rather than jumping into end-to-end runtime or panel work.
- Reusable building blocks already exist in `src/lib/ai` for prompt assembly, persona audit/repair, runtime core normalization, memory context loading, observability, and admin preview UI patterns.
- Immediate blockers for later phases remain unresolved: there is no `orchestrator_runtime_state` table in `schema.sql`, no `inject_persona_tasks` RPC in `schema.sql` or migrations, no `src/app/admin/ai/agent-panel` or `src/app/preview/ai-agent-*` routes yet, and no code currently reads `ai_agent_config` for agent runtime behavior.
- Recommended first implementation slice: define the shared Phase 1 service layer and read models, then land the panel shell/read-only surfaces on top of that shared contract before any write path or orchestrator execution work.
- Documented the agreed layout in [AI_AGENT_INTEGRATION_DEV_PLAN.md](/Users/neven/Documents/projects/llmbook/plans/ai-agent/AI_AGENT_INTEGRATION_DEV_PLAN.md), [src/lib/ai/README.md](/Users/neven/Documents/projects/llmbook/src/lib/ai/README.md), and [src/agents/README.md](/Users/neven/Documents/projects/llmbook/src/agents/README.md): shared ai-agent code lives under `src/lib/ai/agent/`, runtime entrypoints stay flat under `src/agents/`, and reusable UI primitives belong in `src/components/ui`.
- Added the first concrete shared ai-agent module at `src/lib/ai/agent/config/`, including a typed `ai_agent_config` snapshot/parser, a cached provider, and root exports from `src/lib/ai/agent/`.
- Verified the new config layer with `npm test -- src/lib/ai/agent/config/agent-config.test.ts` after first creating the failing test.
- Added `AiAgentOverviewStore` under `src/lib/ai/agent/read-models/` so config, queue summary, usage, checkpoints, latest run, and the current runtime-state placeholder are aggregated through one shared contract.
- Landed the first read-only admin shell at `/admin/ai/agent-panel`, backed by the shared overview store instead of route-local data fetching.
- Added the first `AiAgentPanel` component under `src/components/admin/agent-panel/`, including overview/intake/tasks/logs sections and the minimal operator stats needed for Phase 1.
- Corrected the panel test filename from `.test.tsx` to `.test.ts` because this repo's `vitest.config.ts` only includes `src/**/*.test.ts`.
- Verified the new overview store and panel shell with `npm test -- src/lib/ai/agent/read-models/overview-read-model.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts` after first creating the failing tests.
- Added the first fixture-backed dev page at `/preview/ai-agent-lab`, including lab controls, fixture opportunity preview, generated intake payload preview, and the shared panel shell underneath.
- Extracted `AI_AGENT_PANEL_SECTIONS` into `src/lib/ai/agent/panel-sections.ts` so the panel and future routes do not duplicate section wiring.
- Added `JsonPreviewCard` under `src/components/ui/` for reusable JSON artifact inspection instead of keeping preview-only JSON blocks in admin-specific components.
- Expanded the shared overview snapshot with `recentTasks` and `recentRuns`, then replaced the panel's `Tasks` and `Logs` placeholders with real read-only tables driven by shared runtime data.
- Added a shared mock overview snapshot builder so the panel and preview-lab tests do not duplicate long fixture objects.
- Fixed a preview-lab control issue by switching the dev-only `groupIndexOverride` input to submit-time ref reading, which is simpler and more reliable for this shell than keeping transient controlled state.
- Verified the current read-model, panel, and lab surfaces with `npm test -- src/lib/ai/agent/read-models/overview-read-model.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts`.
- Added shared intake preview builders under `src/lib/ai/agent/intake/` for selector input, selector output, resolved personas, and task candidate shaping, so the preview lab now exercises a real shared contract rather than a one-off payload dump.
- Upgraded `/preview/ai-agent-lab` so it can step through `Generate test input`, `Preview selector output`, `Preview resolved personas`, and `Preview task candidates`, with each stage rendered as a separate JSON artifact panel.
- Kept the intake builders pure and fixture-backed for now, so they are easy to replace with runtime-backed sources later without changing the preview-lab interaction model.
- Verified the intake builder slice with `npm test -- src/lib/ai/agent/intake/intake-preview.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts`.
- Added `AiAgentIntakePreviewStore` under `src/lib/ai/agent/intake/` to build shared runtime-backed notification/public source snapshots from `heartbeat_checkpoints` source streams via `SupabaseHeartbeatSource`.
- Kept the runtime intake store read-only: it only shapes source snapshots plus shared selector input, leaving selector/output/injection execution for later slices.
- Upgraded `/preview/ai-agent-lab` to support both fixture-backed and runtime-backed modes, so the same UI can exercise shared contracts against either static fixtures or live recent source rows.
- Added runtime-preview test fixtures and regression coverage for runtime mode switching in the lab UI.
- Verified the runtime intake slice with `npm test -- src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/intake/intake-preview.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts`.
- Wired the admin panel Intake section to the shared runtime intake previews, so `/admin/ai/agent-panel` and `/preview/ai-agent-lab` now inspect the same notification/public source snapshot model instead of diverging read paths.
- Added `ResolvedPersonaPreviewCard` that adapts the existing `PersonaInfoCard` UI for resolved intake personas, including surfaced `reference_sources` instead of leaving persona resolution as JSON-only output.
- Updated the canonical phase status board in [AI_AGENT_INTEGRATION_DEV_PLAN.md](/Users/neven/Documents/projects/llmbook/plans/ai-agent/AI_AGENT_INTEGRATION_DEV_PLAN.md) so Phase 1 is marked `in_progress` with the current shared intake/read-model slice reflected in the table.
- Verified the admin intake wiring and persona-card preview slice with `npm test -- src/components/admin/agent-panel/AiAgentLabPage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/intake/intake-preview.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts`.
- Added a shared selector prompt artifact by reusing the existing prompt assembly helpers from `src/lib/ai/admin/control-plane-shared.ts`, so selector preview now exposes both the readable assembled prompt and a compact model payload.
- Upgraded `/preview/ai-agent-lab` to show `Selector Prompt Preview` and `Selector Model Payload` panels after selector output is generated, which closes the current biggest parity gap between source snapshots and real LLM-facing artifacts.
- Verified the selector prompt/output slice with `npm test -- src/lib/ai/agent/intake/intake-preview.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts`.
- Added shared `buildReferenceWindow` and `resolvePersonasForReferences` helpers so selector grouping and persona resolution now have an explicit reusable contract instead of relying on incidental array slicing.
- Updated intake preview builders so `groupIndexOverride` now changes the actual selected reference window, and resolved personas are derived from that shared selection result with inactive rows preserved for preview filtering.
- Verified the group-selection/resolver slice with `npm test -- src/lib/ai/agent/intake/intake-preview.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts`.
- Added shared `TaskWritePreview` shaping so candidate previews now include inject-ready dedupe/cooldown expectations, candidate-level inserted/skip outcomes, and preview task IDs without needing the RPC path yet.
- Upgraded `/preview/ai-agent-lab` to render a dedicated `Task Write Preview` artifact, so candidate JSON and expected write behavior are visible together from the same shared contract.
- Verified the candidate/write-preview slice with `npm test -- src/lib/ai/agent/intake/intake-preview.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts`.
- Expanded the shared recent-task snapshot with payload, lease owner/until, dedupe key, and cooldown metadata so the panel can expose task detail without adding a second task-specific fetch path.
- Upgraded the admin panel `Tasks` section to support read-only drill-down via a `Task Detail` card, covering payload JSON, source linkage, result metadata, retry/lease state, and dedupe/cooldown fields from the shared snapshot.
- Verified the task-detail slice with `npm test -- src/lib/ai/agent/read-models/overview-read-model.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/lib/ai/agent/intake/intake-preview.test.ts src/lib/ai/agent/intake/intake-read-model.test.ts`.
- Expanded the shared recent-run snapshot with raw `metadata` so the panel can inspect structured skip reasons, selector summaries, and worker diagnostics without a separate log-detail fetch.
- Upgraded the admin panel `Logs` section to support read-only drill-down via a `Log Detail` card, covering snapshot window, injected counts, skip status, and raw metadata JSON from the shared run snapshot.
- Verified the log-detail slice with `npm test -- src/lib/ai/agent/read-models/overview-read-model.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/lib/ai/agent/intake/intake-preview.test.ts src/lib/ai/agent/intake/intake-read-model.test.ts`.
- Added `src/lib/ai/agent/execution/` with a shared `buildExecutionPreview` artifact builder that combines candidate context, persona context, fixture-backed preview output, and a deterministic write plan under one reusable contract.
- Upgraded `/preview/ai-agent-lab` to render `Execution Persona Context`, `Execution Source Context`, `Execution Write Plan`, and a `PreviewPanel`-backed `Execution Preview`, so preview mode now reaches rendered output, raw response, audit diagnostics, and write planning from the same task candidate.
- Verified the execution-preview slice with `npm test -- src/lib/ai/agent/execution/execution-preview.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/intake/intake-preview.test.ts src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts`.
- Confirmed `interaction-preview-service` can only be reused partially for agent work: prompt assembly helpers, action-output parsers, and persona audit/repair ideas are reusable, but the service itself does not satisfy the agent plan's staged execution contract.
- Refactored `buildExecutionPreview` so the shared contract now exposes explicit `promptInput`, `actualModelPayload`, `rawOutput`, `parsedOutput`, `deterministicChecks`, `auditedOutput`, and `writePlan`, with `PreviewPanel` treated as a derived viewing surface instead of the canonical contract.
- Upgraded `/preview/ai-agent-lab` to render those staged execution artifacts directly, so the lab no longer implies that the legacy `PreviewResult` shape is the agent execution contract.
- Verified the staged execution-contract slice with `npm test -- src/lib/ai/agent/execution/execution-preview.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/intake/intake-preview.test.ts src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts`.
- Added a `Run` section to the shared panel section registry and wired the admin panel to a read-only `buildExecutionPreviewFromTask()` adapter, so operators can inspect staged execution artifacts for the selected queue row without triggering real runner actions.
- Kept the four required runner cards visible but disabled, making the gap between inspectability and true execution explicit instead of hiding the `Run` surface entirely.
- Verified the `Run` section slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/lib/ai/agent/execution/execution-preview.test.ts src/lib/ai/agent/intake/intake-preview.test.ts src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts`.
- Added `TaskInjectionPreview` to the shared intake contract so RPC-style injection results are modeled separately from dedupe/cooldown write expectations.
- Upgraded `/preview/ai-agent-lab` to render `Task Injection Preview`, exposing `inject_persona_tasks`-shaped summary counts plus per-candidate inserted/skip results from the same shared candidate set.
- Verified the injection-preview slice with `npm test -- src/lib/ai/agent/intake/intake-preview.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/execution/execution-preview.test.ts src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts`.
- Wired the admin panel `Intake` section to render `Notification Injection Preview` and `Public Injection Preview`, reusing the same selector/resolver/candidate/write/injection builders from `src/lib/ai/agent/intake/`.
- Kept the operator-side injection surface read-only in this slice, so admins can inspect candidate-to-RPC-result traces without implying that `inject_persona_tasks` is wired for live mutation yet.
- Verified the admin injection-preview slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/lib/ai/agent/intake/intake-preview.test.ts src/lib/ai/agent/execution/execution-preview.test.ts src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts`.
- Added `src/lib/ai/agent/tasks/queue-action-preview.ts` so retry/requeue/mark-dead now have a shared guarded contract instead of being invented ad hoc inside the panel.
- Upgraded the admin panel `Tasks` section with `Queue Actions` and `Queue Action Preview`, exposing status transitions, enable/blocked state, and action payload previews for the selected queue row while keeping buttons non-live.
- Verified the queue-action preview slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/lib/ai/agent/intake/intake-preview.test.ts src/lib/ai/agent/execution/execution-preview.test.ts src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts`.
- Added `AiAgentQueueActionService` plus guarded `POST /api/admin/ai/agent/tasks/[id]/actions`, so queue actions now have a shared admin-only request/response contract even before live mutation is enabled.
- Kept the route in `guarded_preview` mode for this slice: it resolves the selected task, returns the shared action preview for `retry_task | requeue_task | mark_dead`, and explicitly does not mutate queue state yet.
- Verified the guarded queue-action service/API slice with `npm test -- src/lib/ai/agent/tasks/queue-action-service.test.ts 'src/app/api/admin/ai/agent/tasks/[id]/actions/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts src/components/admin/agent-panel/AiAgentLabPage.test.ts src/lib/ai/agent/intake/intake-preview.test.ts src/lib/ai/agent/execution/execution-preview.test.ts src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts`.
- Wired the admin panel `Tasks` surface to the guarded queue-action route, so retry/requeue/mark-dead cards now perform a real admin API round-trip and show the returned `guarded_preview` payload in-panel instead of staying purely static.
- Kept queue actions non-mutating in this slice: the operator can trigger the contract and inspect the server response, but `persona_tasks` rows are still unchanged until the live write path lands.
- Verified the guarded queue-action round-trip slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/tasks/queue-action-service.test.ts 'src/app/api/admin/ai/agent/tasks/[id]/actions/route.test.ts'`.
- Extended `AiAgentQueueActionService` and `POST /api/admin/ai/agent/tasks/[id]/actions` so queue actions now support explicit `mode: preview | execute`, with execute responses returning the updated task snapshot after the shared mutation path runs.
- Upgraded the admin panel `Tasks` section to show separate `Preview` and `Execute` controls per action, and to update the selected local task row after a successful execute response.
- Fixed a contract mismatch while doing this slice: `persona_tasks` does not support a `DEAD` status in schema or TypeScript, so `mark_dead` now maps to terminal `SKIPPED` with `error_message: admin_marked_dead`, and the stale `DEAD` documentation has been corrected.
- Verified the live queue-action slice with `npm test -- src/lib/ai/agent/tasks/queue-action-service.test.ts 'src/app/api/admin/ai/agent/tasks/[id]/actions/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts`.
- Added `AiAgentAdminRunnerService` plus `POST /api/admin/ai/agent/run/[target]`, so `Run` tab actions now have a shared admin contract for `orchestrator_once`, `text_once`, `media_once`, and `compress_once`.
- Implemented honest mode boundaries for runners: `preview` returns shared execution artifacts when available, while `execute` currently returns `guarded_execute` with explicit blockers until real runtime entrypoints land.
- Upgraded the admin panel `Run` tab so each runner card now performs real API round-trips, and the returned runner payload can drive the execution artifact panels instead of relying only on the local selected-task adapter.
- Fixed incidental type issues while doing this slice, including the new runner service/test typing problems; repo-level `tsc` now only reports pre-existing unrelated errors in `.next/types`, `PersonaSelector`, `control-plane-store.patch-persona-profile.test.ts`, `intake-preview.ts`, `active-order-inventory.ts`, and `policy-control-plane.ts`.
- Verified the admin runner slice with `npm test -- src/lib/ai/agent/execution/admin-runner-service.test.ts 'src/app/api/admin/ai/agent/run/[target]/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts`.
- Added shared ai-agent memory builders under `src/lib/ai/agent/memory/` for persona summaries, short-memory row shaping, latest-write preview, compression-batch preview, compression-output preview, and rendered long-memory preview.
- Added `AiAgentMemoryPreviewStore` so `/preview/ai-agent-memory` can try a runtime-backed persona snapshot from `persona_memories` and `persona_tasks`, while still falling back cleanly to fixture-backed previews.
- Landed the first dedicated memory validation page at `/preview/ai-agent-memory`, including persona picker, recent short memories table, canonical long-memory view, latest write preview, compression batch preview, compression output preview, and rendered long-memory preview.
- Added fixture coverage for memory previews plus a UI test for persona switching and section rendering on the new memory page.
- Verified the memory slice with `npm test -- src/lib/ai/agent/memory/memory-preview.test.ts src/components/admin/agent-panel/AiAgentMemoryPage.test.ts`.
- Added `AiAgentMemoryAdminService` plus the first memory admin API routes:
  - `GET /api/admin/ai/agent/memory/personas/[id]`
  - `GET /api/admin/ai/agent/memory/personas/[id]/latest-write-preview`
  - `POST /api/admin/ai/agent/memory/personas/[id]/compression-batch-preview`
  - `POST /api/admin/ai/agent/memory/personas/[id]/preview-compression`
- Upgraded `/preview/ai-agent-memory` so runtime mode now performs real admin round-trips for `Refresh persona memory`, `Preview latest write`, `Preview compression batch`, and `Run compression preview`, and surfaces the returned API payload in-page.
- Fixed an incidental ai-agent contract issue while doing this slice: removed stale `selectedVersion`/`note` fields from `src/lib/ai/agent/intake/intake-preview.ts` to match the current `GlobalPolicyStudioDraft` type.
- Verified the memory API slice with `npm test -- src/lib/ai/agent/memory/memory-admin-service.test.ts 'src/app/api/admin/ai/agent/memory/personas/[id]/route.test.ts' 'src/app/api/admin/ai/agent/memory/personas/[id]/memory-artifacts-routes.test.ts' src/lib/ai/agent/memory/memory-preview.test.ts src/components/admin/agent-panel/AiAgentMemoryPage.test.ts src/lib/ai/agent/intake/intake-preview.test.ts`.
- Added guarded `POST /api/admin/ai/agent/memory/personas/[id]/compress`, so memory persist now has an explicit non-mutating admin contract rather than being only a button in the subplan.
- Added the first main-panel `Memory` section to `/admin/ai/agent-panel`, backed by shared runtime memory previews and the same refresh/latest-write/compression/guarded-compress API round-trips used by the dedicated memory lab.
- Updated the admin panel tests so `Memory` section interactions now cover latest-write preview and guarded persist responses, keeping the operator surface and dev memory page aligned.
- Verified the memory-panel slice with `npm test -- src/lib/ai/agent/memory/memory-admin-service.test.ts 'src/app/api/admin/ai/agent/memory/personas/[id]/memory-artifacts-routes.test.ts' src/components/admin/agent-panel/AiAgentMemoryPage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts`.
- Fixed the nearby repo TypeScript blockers that were adjacent to the ai-agent workstream: `PersonaSelector` now narrows selected personas safely, `active-order-inventory` now maps DB rows through a typed seed before converting to `ActiveOrderModel[]`, and the control-plane persona-profile test no longer indexes a zero-length inferred tuple.
- Restored the missing shared reply-policy contract at `src/agents/task-dispatcher/policy/reply-only-policy.ts`, including fallback defaults and normalization for booleans, whole-number quotas/cooldowns, and clamped similarity thresholds used by `policy-control-plane`.
- Cleared stale generated `.next/types` output that still referenced deleted `review-queue` and board-ban routes, then re-ran `npm exec tsc -- --noEmit --pretty false --incremental false` to confirm the repo returns to a clean typecheck.
- Verified the TypeScript cleanup slice with `npm test -- src/lib/ai/policy/policy-control-plane.test.ts src/lib/ai/admin/control-plane-store.patch-persona-profile.test.ts` and a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Replaced guarded memory compression persistence with a real shared write path in `src/lib/ai/agent/memory/memory-admin-service.ts`: it now deletes existing canonical long-memory rows, inserts a fresh `scope=persona` `long_memory` row from the shared compression preview, deletes compressible short-memory rows, and returns the refreshed persona preview.
- Added `compressNextPersona()` to the shared memory admin service and wired `compress_once` in `src/lib/ai/agent/execution/admin-runner-service.ts` to execute through that same shared persistence path instead of returning `guarded_execute`.
- Updated `/api/admin/ai/agent/memory/personas/[id]/compress`, `/api/admin/ai/agent/run/[target]`, the main panel `Memory` section, and `/preview/ai-agent-memory` so successful compression persistence now refreshes the visible preview state instead of only dumping a guarded placeholder payload.
- Verified the live memory-compress slice with `npm test -- src/lib/ai/agent/memory/memory-admin-service.test.ts src/lib/ai/agent/execution/admin-runner-service.test.ts 'src/app/api/admin/ai/agent/memory/personas/[id]/memory-artifacts-routes.test.ts' 'src/app/api/admin/ai/agent/run/[target]/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts src/components/admin/agent-panel/AiAgentMemoryPage.test.ts` and a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added `src/lib/ai/agent/intake/task-injection-service.ts`, so `notification` and `public` runtime snapshots now reuse the shared selector/resolver/candidate/write-preview pipeline and can persist the inserted subset into `persona_tasks`.
- Added `POST /api/admin/ai/agent/intake/[kind]/inject` and wired the `Intake` section in `/admin/ai/agent-panel` so operators can execute live notification/public injection and immediately see the API result plus newly inserted queue rows flow into `Tasks`.
- Fixed an adjacent contract issue while doing this slice: live injection now maps preview content types onto valid queue task types (`post`, `comment`, `reply`) instead of trying to persist preview-only labels like `mention` directly into `persona_tasks.task_type`.
- Verified the live intake-injection slice with `npm test -- src/lib/ai/agent/intake/task-injection-service.test.ts 'src/app/api/admin/ai/agent/intake/[kind]/inject/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/memory/memory-admin-service.test.ts src/lib/ai/agent/execution/admin-runner-service.test.ts 'src/app/api/admin/ai/agent/memory/personas/[id]/memory-artifacts-routes.test.ts' 'src/app/api/admin/ai/agent/run/[target]/route.test.ts' src/components/admin/agent-panel/AiAgentMemoryPage.test.ts` and a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Upgraded `text_once` in `src/lib/ai/agent/execution/admin-runner-service.ts` from `guarded_execute` to a real shared persistence path for public tasks: comment tasks now insert into `comments`, post tasks insert into `posts`, and the originating `persona_tasks` row is completed with `result_id/result_type`.
- Kept the boundary explicit while doing this slice: live `text_once` currently supports only public tasks; notification-backed text execution still returns a guarded blocker until notification target resolution is implemented.
- Updated the admin panel `Run` tab so successful `text_once` execution refreshes the local selected task row, allowing operators to see `DONE` state and result metadata immediately after the round-trip.
- Verified the live `text_once` slice with `npm test -- src/lib/ai/agent/execution/admin-runner-service.test.ts 'src/app/api/admin/ai/agent/run/[target]/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/intake/task-injection-service.test.ts 'src/app/api/admin/ai/agent/intake/[kind]/inject/route.test.ts' src/lib/ai/agent/memory/memory-admin-service.test.ts 'src/app/api/admin/ai/agent/memory/personas/[id]/memory-artifacts-routes.test.ts' src/components/admin/agent-panel/AiAgentMemoryPage.test.ts` and a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Investigated notification-backed text execution and confirmed the current runtime intake snapshot does not yet preserve enough notification target payload to execute safely; instead of forcing a broken contract, this slice promoted `orchestrator_once` to a real runner path first.
- Upgraded `orchestrator_once` in `src/lib/ai/agent/execution/admin-runner-service.ts` so it now executes a minimal real cycle by chaining live notification injection, live public injection, one public `text_once` execution when available, and one compression pass when available.
- Kept the current boundary explicit: `orchestrator_once` now orchestrates only the already-supported live subpaths, while notification-targeted text execution remains blocked until intake/runtime payloads carry canonical target linkage.
- Verified the live `orchestrator_once` glue slice with `npm test -- src/lib/ai/agent/execution/admin-runner-service.test.ts 'src/app/api/admin/ai/agent/run/[target]/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/intake/task-injection-service.test.ts 'src/app/api/admin/ai/agent/intake/[kind]/inject/route.test.ts' src/lib/ai/agent/memory/memory-admin-service.test.ts 'src/app/api/admin/ai/agent/memory/personas/[id]/memory-artifacts-routes.test.ts' src/components/admin/agent-panel/AiAgentMemoryPage.test.ts` and a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added canonical notification target metadata to runtime intake snapshots in `src/lib/ai/agent/intake/intake-read-model.ts`, carrying `postId`, `commentId`, `parentCommentId`, `context`, and `notificationType` forward from notification payloads.
- Extended shared intake candidate shaping so notification candidates persist that target contract into `persona_tasks.payload.notificationTarget`, which gives runner execution a stable source of reply targets instead of forcing it to reverse-engineer arbitrary notification payloads later.
- Upgraded `text_once` in `src/lib/ai/agent/execution/admin-runner-service.ts` so notification-backed comment/reply tasks can now execute live using `payload.notificationTarget`, while notification-backed post creation remains explicitly guarded.
- Verified the live notification text slice with `npm test -- src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/intake/task-injection-service.test.ts src/lib/ai/agent/execution/admin-runner-service.test.ts 'src/app/api/admin/ai/agent/run/[target]/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/memory/memory-admin-service.test.ts 'src/app/api/admin/ai/agent/memory/personas/[id]/memory-artifacts-routes.test.ts' src/components/admin/agent-panel/AiAgentMemoryPage.test.ts` and a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Completed the remaining notification-backed `text_once` gap: notification post tasks now use `payload.notificationTarget.postId` to resolve the source board and persist a real `posts` row before completing the originating queue task.
- This removes the last notification-specific guard on `text_once`; the shared runner path now covers public comment/post tasks plus notification comment/reply/post tasks, all through the same queue-completion contract.
- Verified the notification post slice with `npm test -- src/lib/ai/agent/intake/intake-read-model.test.ts src/lib/ai/agent/intake/task-injection-service.test.ts src/lib/ai/agent/execution/admin-runner-service.test.ts 'src/app/api/admin/ai/agent/run/[target]/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/memory/memory-admin-service.test.ts 'src/app/api/admin/ai/agent/memory/personas/[id]/memory-artifacts-routes.test.ts' src/components/admin/agent-panel/AiAgentMemoryPage.test.ts` and a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Upgraded `media_once` in `src/lib/ai/agent/execution/admin-runner-service.ts` from a guarded placeholder into a real shared write path that reuses the staged execution preview contract and persists `media.status='PENDING_GENERATION'` rows from `writePlan.mediaWrite`.
- Kept the boundary explicit and safe while doing this slice: `media_once` now requires a completed text task with `result_id/result_type` plus an execution preview that actually requests an image, so the inserted media row always links back to a real `posts` or `comments` owner.
- Updated the admin panel `Run` copy so runner-card descriptions now reflect the real state of `text_once`, `media_once`, `compress_once`, and `orchestrator_once` instead of implying media was still fully guarded.
- Verified the live media-runner slice with `npm test -- src/lib/ai/agent/execution/admin-runner-service.test.ts 'src/app/api/admin/ai/agent/run/[target]/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts` and a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Extended `orchestrator_once` in `src/lib/ai/agent/execution/admin-runner-service.ts` so the minimal live cycle is now `notification injection -> public injection -> first text execution -> optional media job dispatch -> compression`, rather than stopping at text persistence.
- Updated the shared runner/orchestrator contract so both top-level runner responses and nested `orchestratorResult` now surface the executed media job when one is queued from an image-backed text task.
- Verified the orchestrator+media slice with `npm test -- src/lib/ai/agent/execution/admin-runner-service.test.ts 'src/app/api/admin/ai/agent/run/[target]/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts` and a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added `src/lib/ai/agent/execution/media-job-service.ts`, a shared media pipeline that reuses the staged execution contract, resolves the active `image_generation` route, calls the configured xAI image model, uploads the generated asset to Supabase storage, and marks the `media` row `DONE` with URL and metadata.
- Upgraded `media_once` in `src/lib/ai/agent/execution/admin-runner-service.ts` to use that shared service, so the runner now completes real generation and upload work instead of stopping at `PENDING_GENERATION`; when an existing completed row already matches owner+persona+prompt, it reuses that result instead of regenerating.
- Added focused regression coverage in `src/lib/ai/agent/execution/media-job-service.test.ts` for reuse, happy-path completion, and failure-to-FAILED behavior, and updated runner/panel copy/tests to match the new `DONE/url` media result contract.
- Verified the live media-generation slice with `npm test -- src/lib/ai/agent/execution/media-job-service.test.ts src/lib/ai/agent/execution/admin-runner-service.test.ts 'src/app/api/admin/ai/agent/run/[target]/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts` and a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Extended the comment data path so `RawComment` / `FormattedComment` now carry attached media, and both `GET /api/posts/[id]/comments` and `GET /api/profile/comments` now select `media(url, width, height)` instead of dropping comment-linked image rows on the floor.
- Updated `CommentItem` to render attached images below the comment body, which gives ai-agent-generated comment media a real frontend outlet without rewriting the comment markdown contract.
- Added a dedicated `Media Result` + `Run Media Result JSON` inspect surface in `/admin/ai/agent-panel` `Run`, so operators can see completed media URLs and ownership without digging only through the raw runner payload.
- Added regression coverage in `src/lib/posts/query-builder.test.ts` and `src/components/comment/CommentItem.test.ts`, and cleaned the React `act(...)` test-environment warning while re-verifying the slice.
- Verified the media-consumption slice with `npm test -- src/lib/posts/query-builder.test.ts src/components/comment/CommentItem.test.ts src/lib/ai/agent/execution/media-job-service.test.ts src/lib/ai/agent/execution/admin-runner-service.test.ts 'src/app/api/admin/ai/agent/run/[target]/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Extended `AiAgentOverviewSnapshot` with `recentMediaJobs`, added DB-backed loading from `media`, and surfaced persona identity on those rows so generated-media status is available through the same shared read-model used by `/admin/ai/agent-panel`.
- Updated `/admin/ai/agent-panel` `Logs` to include a media-jobs table plus drill-down detail, so operators can inspect `PENDING_GENERATION | RUNNING | DONE | FAILED`, owner linkage, prompt, and URL outside the transient `Run` response.
- Updated `PostRow` to render `thumbnailUrl` as a visible card image, which makes generated post media show up in feed/list surfaces instead of being silently available only in transformed data.
- Added regression coverage in `src/lib/ai/agent/read-models/overview-read-model.test.ts` and `src/components/post/PostRow.test.ts`, then re-verified with `npm test -- src/lib/ai/agent/read-models/overview-read-model.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts src/components/post/PostRow.test.ts src/lib/posts/query-builder.test.ts src/components/comment/CommentItem.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added `PostMediaGallery` and upgraded post detail rendering to use richer media metadata from `getPostForDetail`, so generated post media is now presented as a reusable gallery instead of page-local inline `<img>` blocks.
- Added `AiAgentMediaAdminService` plus `GET /api/admin/ai/agent/media/jobs`, giving admin surfaces a dedicated recent-media-jobs contract with status summaries instead of overloading overview snapshots or runner responses.
- Updated `/admin/ai/agent-panel` `Logs` so media jobs live in client state and can be refreshed against the new admin route, which makes `PENDING_GENERATION | RUNNING | DONE | FAILED` inspection less dependent on the initial server render.
- Fixed an adjacent media contract issue while doing this slice: `AiAgentMediaExecutionPersistedResult.status` now preserves the real row status union instead of collapsing everything non-`DONE` into `PENDING_GENERATION`.
- Verified the post-detail/admin-media slice with `npm test -- src/components/post/PostMediaGallery.test.ts src/components/post/PostRow.test.ts src/lib/ai/agent/execution/media-admin-service.test.ts 'src/app/api/admin/ai/agent/media/jobs/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Extended `AiAgentMediaAdminService` with `getJobDetail()`, adding a shared owner-linked detail contract that resolves selected media rows back to their post/comment owner, board slug, title, excerpt, status, and navigable path.
- Added `GET /api/admin/ai/agent/media/jobs/[id]`, so admin surfaces can inspect a specific media job without over-fetching or reusing the recent-jobs list response shape for detail views.
- Upgraded `/admin/ai/agent-panel` `Logs` so selected media rows can load owner detail on demand, showing image preview, owner type, title, excerpt, and path alongside the raw job JSON.
- Verified the owner-detail media slice with `npm test -- src/lib/ai/agent/execution/media-admin-service.test.ts 'src/app/api/admin/ai/agent/media/jobs/route.test.ts' 'src/app/api/admin/ai/agent/media/jobs/[id]/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts src/components/post/PostMediaGallery.test.ts src/components/post/PostRow.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added `retry_generation` as a shared admin media-job action contract in `src/lib/ai/agent/execution/media-job-action-service.ts`, with explicit preview semantics for `FAILED` / `PENDING_GENERATION` rows and clear blockers for `RUNNING` / `DONE`.
- Extended `AiAgentMediaJobService` with `rerunJobById()`, `loadJobById()`, and `markJobRunning()` so retry execution can regenerate an existing media row safely without inventing a second persistence path.
- Added `POST /api/admin/ai/agent/media/jobs/[id]/actions` and wired `/admin/ai/agent-panel` `Logs` so operators can preview or execute `retry_generation`, then see the selected media row and owner detail refresh to the new state.
- Verified the media-action slice with `npm test -- src/lib/ai/agent/execution/media-job-action-service.test.ts src/lib/ai/agent/execution/media-admin-service.test.ts 'src/app/api/admin/ai/agent/media/jobs/route.test.ts' 'src/app/api/admin/ai/agent/media/jobs/[id]/route.test.ts' 'src/app/api/admin/ai/agent/media/jobs/[id]/actions/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts src/components/post/PostMediaGallery.test.ts src/components/post/PostRow.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Extended `AiAgentMediaAdminService.listRecentJobs()` so the shared media-jobs list contract now accepts `limit`, `status`, and `query`, with server-side status filtering and shared search across persona, owner ids, prompt, URL, and job id.
- Updated `GET /api/admin/ai/agent/media/jobs` to parse and validate `status` and `query`, which gives admin surfaces one canonical list/filter route instead of pushing filter semantics into panel-only state.
- Upgraded `/admin/ai/agent-panel` `Logs` media controls from a plain refresh button to live status-filter + apply/reset controls wired through the route querystring, so operators can narrow the list to failure recovery cases before drilling into detail or retry actions.
- Verified the media-list filtering slice with `npm test -- src/lib/ai/agent/execution/media-admin-service.test.ts src/lib/ai/agent/execution/media-job-action-service.test.ts 'src/app/api/admin/ai/agent/media/jobs/route.test.ts' 'src/app/api/admin/ai/agent/media/jobs/[id]/route.test.ts' 'src/app/api/admin/ai/agent/media/jobs/[id]/actions/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Completed the panel-side media list controls so `query` now updates through the same admin round-trip as `status`, and `limit` is promoted to explicit client state instead of being hardcoded at `12`.
- Added a `Load more` path to `/admin/ai/agent-panel` `Logs`, which expands the media-job history window in `12`-row steps through the shared route contract instead of pretending an unpaginated recent slice is enough for ops work.
- Fixed the adjacent panel input issue by syncing the search box through both `onInput` and `onChange`, which makes the controlled query state reliable in both runtime usage and jsdom regression tests.
- Verified the media-query/history slice with `npm test -- src/lib/ai/agent/execution/media-admin-service.test.ts src/lib/ai/agent/execution/media-job-action-service.test.ts 'src/app/api/admin/ai/agent/media/jobs/route.test.ts' 'src/app/api/admin/ai/agent/media/jobs/[id]/route.test.ts' 'src/app/api/admin/ai/agent/media/jobs/[id]/actions/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added canonical media retry reason codes in `src/lib/ai/agent/execution/media-job-action-service.ts`, so `retry_generation` preview now distinguishes `DONE_ROW`, `ACTIVE_ROW`, `MISSING_PERSONA`, `MISSING_OWNER_LINKAGE`, `MISSING_IMAGE_PROMPT`, and `RETRY_READY`.
- Updated `/admin/ai/agent-panel` `Logs` media action UI to show the current retry `reasonCode` inline, instead of hiding that state only inside the JSON artifact card.
- Verified the media-reason-code slice with `npm test -- src/lib/ai/agent/execution/media-job-action-service.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts 'src/app/api/admin/ai/agent/media/jobs/[id]/actions/route.test.ts'` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added `AiAgentMediaJobActionBlockedResponse` and `AiAgentMediaJobActionBlockedError`, so blocked media-action executes now return the same structured contract shape as preview instead of collapsing back to free-form route errors.
- Updated `POST /api/admin/ai/agent/media/jobs/[id]/actions` to return `409` JSON with canonical `mode="blocked_execute"` payloads, including `reasonCode`, when retry execution is disallowed.
- Extended `/admin/ai/agent-panel` `Logs` media action UI to read `ApiError.details` and keep rendering the blocked execute `reasonCode` inline, so operators can see why execute failed without opening raw JSON or losing context after a failed round-trip.
- Verified the structured blocked-response slice with `npm test -- src/lib/ai/agent/execution/media-job-action-service.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts 'src/app/api/admin/ai/agent/media/jobs/[id]/actions/route.test.ts'` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Corrected the canonical phase board in [AI_AGENT_INTEGRATION_DEV_PLAN.md](/Users/neven/Documents/projects/llmbook/plans/ai-agent/AI_AGENT_INTEGRATION_DEV_PLAN.md) so it now matches the real implementation state: Phases 1-4 are recorded as `done`, Phase 5 remains the single active `in_progress` phase, and Phase 6 remains pending final end-to-end validation.
- Synced the active-session tracker and panel subplan wording to reflect that this slice was documentation/progress reconciliation work, not another functional runtime change.
- Added shared memory artifact detail builders in [memory-artifact-detail.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/memory/memory-artifact-detail.ts), so latest-write, compression-batch, compression-output, and rendered-long-memory modal content now comes from one reusable contract.
- Added a reusable [ArtifactDetailModal.tsx](/Users/neven/Documents/projects/llmbook/src/components/ui/ArtifactDetailModal.tsx) under `src/components/ui`, then wired both [AiAgentMemoryPage.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentMemoryPage.tsx) and [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) to open modal/detail views for memory artifacts instead of relying only on inline JSON cards.
- This slice closes a visible Phase 5 gap from the main plan: latest write, compression input/output, rendered long memory, and cleanup consequences now have modal/detail parity in both the dedicated memory lab and the main operator panel.
- Verified the memory-modal slice with `npm test -- src/components/admin/agent-panel/AiAgentMemoryPage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Extended [AiAgentMemoryPersistedCompressResponse](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/memory/memory-admin-service.ts) with `verificationTrace`, so persisted compression now explicitly reports the canonical long-memory row that was written plus deleted/protected short-memory IDs.
- Updated [AiAgentMemoryPage.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentMemoryPage.tsx) and [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) to render `Memory Verification Trace` after successful persistence instead of leaving operators to infer writes only from the raw API payload.
- Fixed the adjacent type fallout in [admin-runner-service.test.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/execution/admin-runner-service.test.ts) so the expanded memory contract stays aligned across runner fixtures.
- Verified the persisted-result verification slice with `npm test -- src/lib/ai/agent/memory/memory-admin-service.test.ts src/components/admin/agent-panel/AiAgentMemoryPage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts 'src/app/api/admin/ai/agent/memory/personas/[id]/memory-artifacts-routes.test.ts'` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added [persistLatestWrite()](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/memory/memory-admin-service.ts) plus `AiAgentMemoryWriteResponse`, so `latestWritePreview` now has a real shared persist path instead of stopping at read-only inspection.
- Added [persist-latest-write route](/Users/neven/Documents/projects/llmbook/src/app/api/admin/ai/agent/memory/personas/[id]/persist-latest-write/route.ts), and wired both [AiAgentMemoryPage.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentMemoryPage.tsx) and [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) with a `Persist latest write` action plus verification trace display.
- This closes another concrete Phase 5 gap: latest-write preview and latest-write persistence now share one contract with explicit persisted row verification, instead of only compression having end-to-end write verification.
- Verified the latest-write persist slice with `npm test -- src/lib/ai/agent/memory/memory-admin-service.test.ts src/components/admin/agent-panel/AiAgentMemoryPage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts 'src/app/api/admin/ai/agent/memory/personas/[id]/memory-artifacts-routes.test.ts'` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added shared [memory-lineage.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/memory/memory-lineage.ts), which summarizes selected task id/persona, latest-write selected task id, persisted latest-write row id, and persisted compression long-memory id into one operator-readable lineage payload.
- Updated [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) to render `Task Memory Lineage` inside the `Memory` section, so operators can see when the current selected task diverges from the task that produced the latest memory-write trace.
- Fixed the incidental null-guard bug in the lineage helper and a type import mismatch while landing this slice.
- Verified the lineage slice with `npm test -- src/lib/ai/agent/memory/memory-lineage.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Extended [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so the `Run` section now renders the same shared `Task Memory Lineage` payload as `Memory`, removing the need for operators to cross-check runner output in another tab.
- Synced runner-side persisted compression results back into panel memory state, so `compress_once` and `orchestrator_once` now update the selected memory persona preview plus verification trace through the same lineage contract used by the memory admin routes.
- Verified the run-lineage parity slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Extended [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so the `Run` section now also exposes `Run Memory Verification` and raw `Run Compression Result`, letting operators inspect persisted compression checks without leaving the runner surface.
- Fixed a real runner-state bug while landing this slice: orchestrator execute responses previously merged injected tasks before their updated text-persisted row, which left the `Tasks` tab showing stale `PENDING` status for the same task id; the merge order is now deterministic and prefers the executed update.
- Verified the runner-sync slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Extended [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so runner text/orchestrator execution now rebuilds touched persona memory previews through shared [buildMemoryPersonaPreview()](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/memory/memory-preview.ts), instead of leaving latest-write preview and lineage tied to stale pre-execution tasks.
- This closes another concrete Phase 5 parity gap: after `text_once`, `Task Memory Lineage` now updates `latestWriteSelectedTaskId` and `latestWriteMatchesSelectedTask` immediately in the `Run` tab, which makes the “task result becomes memory-write candidate” path inspectable without a manual refresh.
- Added shared [pm-walkthrough-checklist.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/pm-walkthrough-checklist.ts), which maps `Overview`, `Intake`, `Tasks`, `Run`, `Memory`, and `Logs` onto one live PM-facing checklist derived from panel evidence instead of leaving acceptance criteria only in plan docs.
- Updated [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so `Overview` now renders `PM Walkthrough Checklist` with both partial and ready sign-off states, alongside the already-live `Continuous Runtime Checkpoint`.
- Verified the PM-checklist slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/pm-walkthrough-checklist.test.ts src/lib/ai/agent/continuous-runtime-checkpoint.test.ts src/lib/ai/agent/runtime-control-service.test.ts 'src/app/api/admin/ai/agent/runtime/[action]/route.test.ts' src/lib/ai/agent/readiness-summary.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts src/lib/ai/agent/operator-flow-trace.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- The remaining ai-agent backlog is now PM/sign-off execution work rather than missing operator surface delivery: run the explicit acceptance walkthrough against the now-live panel, then close the remaining wording drift in plan/docs if PM finds anything ambiguous.
- Verified the runner-memory-preview slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Extended [memory-lineage.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/memory/memory-lineage.ts) with a shared `Task Memory Outcome Trace`, which summarizes stage readiness and persisted ids across selected task, latest-write candidate, latest-write persistence, and compression persistence.
- Updated [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so both `Run` and `Memory` now surface `Task Memory Outcome Trace`, giving operators one higher-level JSON contract for the full task-to-memory path instead of forcing them to cross-read lineage plus verification cards.
- Verified the outcome-trace slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Fixed a concrete multi-persona parity bug in [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx): latest-write persisted rows, compression persisted rows, and verification traces are now derived per active persona, so switching from `persona-1` to `persona-2` no longer leaves stale `memory-write-*` or `long-memory-*` ids visible in the wrong persona view.
- Verified the persona-scope slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added shared [operator-flow-trace.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/operator-flow-trace.ts), which turns `injectionResponse`, `runnerResponse`, and `Task Memory Outcome Trace` into one operator-facing end-to-end summary with explicit `intakeCompleted`, `executionCompleted`, `latestWritePersisted`, and `compressionPersisted` stage flags.
- Updated [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so both `Run` and `Memory` surface `Operator Flow Trace`, giving the operator one compact cross-stage contract instead of forcing them to hop between `Injection API Result`, `Run API Result`, and memory cards to infer flow progress.
- Verified the operator-flow slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts src/lib/ai/agent/operator-flow-trace.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added reusable [PromptDetailModal.tsx](/Users/neven/Documents/projects/llmbook/src/components/ui/PromptDetailModal.tsx), which shows prompt input, assembled prompt, and model payload, and includes a working `Copy Prompt` action without coupling the panel to the legacy control-plane preview UI.
- Updated [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so the `Run` section now exposes `View Prompt`, opening a modal backed directly by the shared execution preview contract. This closes a concrete Phase 6 gap for worker-stage prompt visibility on the main operator surface.
- Verified the run-prompt-modal slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts src/lib/ai/agent/operator-flow-trace.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Updated [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so `Notification Injection Preview` and `Public Injection Preview` now each expose `View Prompt`, opening the same reusable prompt modal with selector `promptPreview`, `selectorInput`, and actual model payload.
- This closes another concrete Phase 6 prompt-visibility gap: the main operator panel can now inspect and copy intake selector prompts directly, instead of leaving selector prompt debugging only to `/preview/ai-agent-lab`.
- Verified the intake-prompt-modal slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts src/lib/ai/agent/operator-flow-trace.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added shared [readiness-summary.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/readiness-summary.ts), which turns overview snapshot state into a stable operator-facing readiness contract covering runtime availability, queue backlog, token/image quota headroom, checkpoint coverage, and latest run presence.
- Updated [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so `Overview` now includes `Operator Readiness`, giving operators one explicit readiness judgment instead of forcing them to infer safety from multiple separate cards.
- Verified the overview-readiness slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/readiness-summary.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts src/lib/ai/agent/operator-flow-trace.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added shared [runtime-control-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/runtime-control-service.ts) plus [POST /api/admin/ai/agent/runtime/[action]](/Users/neven/Documents/projects/llmbook/src/app/api/admin/ai/agent/runtime/[action]/route.ts), giving `pause`, `resume`, and `run_cycle` one admin contract even though `orchestrator_runtime_state` is still not implemented in schema/runtime.
- Updated [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so `Overview` now exposes `Pause runtime`, `Resume runtime`, and `Force run cycle`, and keeps rendering the structured blocked payload even when the API returns `HTTP 409`.
- Fixed two regressions while landing this slice: blocked runtime-control JSON had been hidden behind the `ApiError` message in the panel, and the new route test was using `Request` instead of `NextRequest`, which broke clean repo `tsc`.
- Verified the runtime-control slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/runtime-control-service.test.ts 'src/app/api/admin/ai/agent/runtime/[action]/route.test.ts' src/lib/ai/agent/readiness-summary.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts src/lib/ai/agent/operator-flow-trace.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Extended [AiAgentRuntimeStateSnapshot](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/read-models/overview-read-model.ts) so runtime state now carries `paused`, `leaseOwner`, `leaseUntil`, `cooldownUntil`, `lastStartedAt`, and `lastFinishedAt`, instead of collapsing readiness to a single status string.
- Updated [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so `Overview` now renders those lease/cooldown details and updates the runtime-state card plus readiness summary from the latest runtime-control response instead of staying pinned to the initial server snapshot.
- Added regression coverage in [AiAgentPanel.test.ts](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.test.ts) for successful runtime-control execute, proving that lease owner, cooldown visibility, and status text really change in-panel after `Pause runtime`.
- Verified the runtime-state visibility slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/runtime-control-service.test.ts 'src/app/api/admin/ai/agent/runtime/[action]/route.test.ts' src/lib/ai/agent/readiness-summary.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts src/lib/ai/agent/operator-flow-trace.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added shared [buildRuntimeControlGuard()](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/runtime-control-service.ts), so pause/resume/run-cycle now share one paused/cooldown/unavailable gating contract instead of scattering those checks across service and UI logic.
- Updated [AiAgentRuntimeControlService](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/runtime-control-service.ts) to return canonical blocked reason codes for `already_paused`, `not_paused`, `runtime_paused`, and `cooldown_active`, not just `runtime_state_unavailable`.
- Updated [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so `Overview` now shows the shared guard summaries inline and disables actions that are already semantically blocked by pause/cooldown state, while still allowing unavailable-state blocked inspection via the route.
- Verified the runtime-guard slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/runtime-control-service.test.ts 'src/app/api/admin/ai/agent/runtime/[action]/route.test.ts' src/lib/ai/agent/readiness-summary.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts src/lib/ai/agent/operator-flow-trace.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Promoted the largest panel regression in [AiAgentPanel.test.ts](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.test.ts) into an explicit Phase 6 operator walkthrough, so one test now proves notification intake, public comment/post intake, task execution, and memory verification happen in a single UI flow.
- Extended that walkthrough to execute both `Inject notification tasks` and `Inject public tasks`, and to assert that both `public-comment` and `public-post` opportunities are visible before the runner stage begins.
- Fixed the walkthrough expectation drift caused by current panel behavior: after public injection, the selected task now correctly advances to the new public comment task, so the canonical flow now validates execution/memory parity against `task-new-public-comment-1` instead of the older notification task id.
- Verified the walkthrough-verification slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/runtime-control-service.test.ts 'src/app/api/admin/ai/agent/runtime/[action]/route.test.ts' src/lib/ai/agent/readiness-summary.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts src/lib/ai/agent/operator-flow-trace.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Reconciled the current progress docs and confirmed they already reflect the latest walkthrough/runtime-control slices; the remaining development queue is now PM/sign-off closure work rather than missing core surfaces.
- Upgraded [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) `Log Detail` with `Structured Diagnostics`, so selector summary, worker summary, parser status/issues, and repair state are rendered as first-class operator fields instead of being left inside raw run metadata JSON.
- Extended [mock-overview-snapshot.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/testing/mock-overview-snapshot.ts) so recent-run fixtures now include parser/repair metadata, which lets the panel regression prove parser-repair visibility instead of only checking `cooldown_active`.
- Added executed/skipped log diagnostics coverage in [AiAgentPanel.test.ts](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.test.ts), including explicit assertions for selector counts, worker counts, parser repair status, parser issues, and skipped-run cooldown metadata.
- Verified the logs-verification slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/runtime-control-service.test.ts 'src/app/api/admin/ai/agent/runtime/[action]/route.test.ts' src/lib/ai/agent/readiness-summary.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts src/lib/ai/agent/operator-flow-trace.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added shared [continuous-runtime-checkpoint.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/continuous-runtime-checkpoint.ts), which turns readiness, operator walkthrough state, memory persistence, and logs diagnostics into one explicit sign-off contract.
- Updated [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so `Overview` now includes `Continuous Runtime Checkpoint`, with check rows for runtime readiness, operator walkthrough, memory persistence, and logs diagnostics.
- Added coverage in [continuous-runtime-checkpoint.test.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/continuous-runtime-checkpoint.test.ts) and [AiAgentPanel.test.ts](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.test.ts) for both the default blocked/attention state and a fully green `Ready For Continuous Runtime` path.
- Fixed two test regressions while landing this slice: the ready-case flow needed a real latest-write persist before compression, and tab/runner button references had to be reacquired after section changes so the executed runner response actually stayed in state.
- Verified the continuous-runtime-checkpoint slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/continuous-runtime-checkpoint.test.ts src/lib/ai/agent/runtime-control-service.test.ts 'src/app/api/admin/ai/agent/runtime/[action]/route.test.ts' src/lib/ai/agent/readiness-summary.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts src/lib/ai/agent/operator-flow-trace.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added shared [pm-walkthrough-checklist.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/pm-walkthrough-checklist.ts), which turns the documented manual validation flow into a live checklist over `Overview`, `Intake`, `Tasks`, `Run`, `Memory`, and `Logs`.
- Updated [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so `Overview` now includes `PM Walkthrough Checklist`, making PM sign-off status visible from the same operator evidence that already drives runtime readiness and continuous-runtime checkpointing.
- Added coverage in [pm-walkthrough-checklist.test.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/pm-walkthrough-checklist.test.ts) and extended [AiAgentPanel.test.ts](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.test.ts) so both partial and ready PM walkthrough states are explicitly verified.
- Verified the PM checklist slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/pm-walkthrough-checklist.test.ts src/lib/ai/agent/continuous-runtime-checkpoint.test.ts src/lib/ai/agent/runtime-control-service.test.ts 'src/app/api/admin/ai/agent/runtime/[action]/route.test.ts' src/lib/ai/agent/readiness-summary.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts src/lib/ai/agent/operator-flow-trace.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added shared [pm-acceptance-summary.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/pm-acceptance-summary.ts), which turns the continuous-runtime checkpoint plus walkthrough checklist into one PM-facing acceptance decision with recommendation, completed evidence, and outstanding items.
- Updated [AiAgentPanel.tsx](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.tsx) so `Overview` now includes `PM Acceptance Summary`, making the next PM action explicit instead of requiring interpretation across multiple cards.
- Added coverage in [pm-acceptance-summary.test.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/pm-acceptance-summary.test.ts) and extended [AiAgentPanel.test.ts](/Users/neven/Documents/projects/llmbook/src/components/admin/agent-panel/AiAgentPanel.test.ts) so both blocked and ready PM acceptance states are explicitly verified.
- Verified the PM acceptance-summary slice with `npm test -- src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/pm-acceptance-summary.test.ts src/lib/ai/agent/pm-walkthrough-checklist.test.ts src/lib/ai/agent/continuous-runtime-checkpoint.test.ts src/lib/ai/agent/runtime-control-service.test.ts 'src/app/api/admin/ai/agent/runtime/[action]/route.test.ts' src/lib/ai/agent/readiness-summary.test.ts src/lib/ai/agent/memory/memory-lineage.test.ts src/lib/ai/agent/operator-flow-trace.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Reconciled the canonical plan against the actual implementation and recorded the true gap split: operator/manual validation is mostly aligned, but continuous runtime is not fully aligned yet.
- Documented the three largest remaining implementation gaps in the canonical plan: real `orchestrator_runtime_state` persistence and runtime controls, a true `inject_persona_tasks` SQL/RPC contract, and real ai-agent runtime entrypoints under `src/agents/*`.
- Synced the panel subplan wording so it no longer implies that panel completeness automatically means continuous-runtime completeness.
- Current remaining backlog is now split into two buckets: actual PM acceptance/sign-off on the operator surfaces, and real runtime completion work for continuous/background execution.
- Added [20260330123000_orchestrator_runtime_state.sql](/Users/neven/Documents/projects/llmbook/supabase/migrations/20260330123000_orchestrator_runtime_state.sql), plus matching [schema.sql](/Users/neven/Documents/projects/llmbook/supabase/schema.sql) and [verification.sql](/Users/neven/Documents/projects/llmbook/supabase/verification.sql) updates, so `orchestrator_runtime_state` now exists as a persisted singleton table with an initial `global` row.
- Added shared [runtime-state-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/runtime-state-service.ts), which is now the single contract for loading runtime state and persisting `pause`, `resume`, and `run_cycle` transitions.
- Updated [overview-read-model.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/read-models/overview-read-model.ts) so the panel overview now reads real runtime state through the shared service instead of hardcoded placeholder snapshots.
- Updated [runtime-control-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/runtime-control-service.ts) so successful `pause`, `resume`, and `run_cycle` requests now write real persisted state transitions instead of returning `control_not_wired`.
- Verified the runtime-state persistence slice with `npm test -- src/lib/ai/agent/runtime-state-service.test.ts src/lib/ai/agent/runtime-control-service.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts 'src/app/api/admin/ai/agent/runtime/[action]/route.test.ts' src/lib/ai/agent/readiness-summary.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Extended that same migration/schema/verification slice with a real `inject_persona_tasks(candidates jsonb)` RPC, so injection no longer depends on app-side `query/filter/insert`.
- Reworked [task-injection-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/intake/task-injection-service.ts) to send all candidates to the RPC, let DB-side dedupe/cooldown decide the outcome, then rebuild the existing panel response shape from actual RPC results plus the inserted `persona_tasks` rows.
- Verified the injection-contract slice with `npm test -- src/lib/ai/agent/intake/task-injection-service.test.ts 'src/app/api/admin/ai/agent/intake/[kind]/inject/route.test.ts' src/components/admin/agent-panel/AiAgentPanel.test.ts src/lib/ai/agent/runtime-state-service.test.ts src/lib/ai/agent/runtime-control-service.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts 'src/app/api/admin/ai/agent/runtime/[action]/route.test.ts' && npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added [20260330160000_orchestrator_runtime_lease_functions.sql](/Users/neven/Documents/projects/llmbook/supabase/migrations/20260330160000_orchestrator_runtime_lease_functions.sql), plus matching [schema.sql](/Users/neven/Documents/projects/llmbook/supabase/schema.sql) and [verification.sql](/Users/neven/Documents/projects/llmbook/supabase/verification.sql) updates, so `orchestrator_runtime_state` now has DB-backed `claim`, `heartbeat`, and `release` helpers instead of only admin-triggered state transitions.
- Extended [runtime-state-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/runtime-state-service.ts) with shared `claimLease`, `heartbeatLease`, and `releaseLease` methods, which are now the common runtime-state contract for overview reads, runtime-control, and background orchestration.
- Tightened [runtime-control-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/runtime-control-service.ts) so `run_cycle` is blocked while another active runtime lease is still held, instead of allowing operators to force a second cycle into an already-running orchestrator.
- Added shared [orchestrator-loop-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/orchestrator/orchestrator-loop-service.ts), which reuses the live `orchestrator_once` execute path inside a real lease + heartbeat + release loop instead of inventing another execution path.
- Added the first real ai-agent background entrypoint at [src/agents/orchestrator/runner.ts](/Users/neven/Documents/projects/llmbook/src/agents/orchestrator/runner.ts), so continuous runtime no longer depends only on admin/manual routes.
- Verified the background-runtime slice with `npm test -- src/lib/ai/agent/runtime-state-service.test.ts src/lib/ai/agent/orchestrator/orchestrator-loop-service.test.ts src/lib/ai/agent/runtime-control-service.test.ts 'src/app/api/admin/ai/agent/runtime/[action]/route.test.ts' src/lib/ai/agent/read-models/overview-read-model.test.ts src/components/admin/agent-panel/AiAgentPanel.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Updated [task-queue.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/task-queue/task-queue.ts) and [supabase-task-queue-store.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/task-queue/supabase-task-queue-store.ts) so queue claims now respect the planned text priority `reply -> comment -> post` instead of blindly taking the oldest row regardless of task kind.
- Added shared [text-lane-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/execution/text-lane-service.ts), which recovers timed-out leases, claims the next queued text task, heartbeats the task lease while execution runs, and reuses the shared `text_once` persistence path for queue-driven drain.
- Added the first background text-drain entrypoint at [src/agents/text-worker/runner.ts](/Users/neven/Documents/projects/llmbook/src/agents/text-worker/runner.ts), so background runtime is now split into inject-only orchestrator phase plus queue-driven text execution instead of one `orchestrator_once` chain.
- Refactored [orchestrator-loop-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/orchestrator/orchestrator-loop-service.ts) to run a shared inject-only [orchestrator-phase-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/orchestrator/orchestrator-phase-service.ts), which makes the background orchestrator align with Phase A of the runtime plan instead of inlining text/media/compression execution.
- Verified the queue-driven drain slice with `npm test -- src/lib/ai/task-queue/task-queue.test.ts src/lib/ai/agent/execution/text-lane-service.test.ts src/lib/ai/agent/orchestrator/orchestrator-loop-service.test.ts src/lib/ai/agent/runtime-state-service.test.ts src/lib/ai/agent/runtime-control-service.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Current remaining runtime backlog is now concentrated on dedicated background media/compression entrypoints and moving those paths out of admin/manual execute flows, not on orchestrator inject-only phase or text-lane queue drain.
- Synced the canonical integration plan, panel subplan, and `src/agents/*` READMEs so they now describe the live inject-only orchestrator plus queue-driven text worker model, rather than the older inline `orchestrator_once` chaining description.
- Added [20260330170000_media_retry_metadata.sql](/Users/neven/Documents/projects/llmbook/supabase/migrations/20260330170000_media_retry_metadata.sql) and synced [schema.sql](/Users/neven/Documents/projects/llmbook/supabase/schema.sql), so `media` rows now carry `retry_count`, `max_retries`, `next_retry_at`, and `last_error` for background image-worker backoff.
- Refactored [media-job-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/execution/media-job-service.ts) into shared `ensure pending job` plus `execute claimed row` paths, so text lane, retry actions, manual media execution, and background media drain now reuse the same generation/upload/failure-backoff contract.
- Added shared [media-lane-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/execution/media-lane-service.ts) and a real background entrypoint at [src/agents/media-worker/runner.ts](/Users/neven/Documents/projects/llmbook/src/agents/media-worker/runner.ts), so Phase B now has a dedicated media worker instead of depending only on admin/manual `media_once`.
- Extended [text-lane-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/execution/text-lane-service.ts) so successful background text execution queues pending media rows for image-backed tasks, which gives the new media worker real queue input without reintroducing inline image generation into the text lane.
- Verified the media-worker slice with `npm test -- src/lib/ai/agent/execution/media-job-service.test.ts src/lib/ai/agent/execution/media-lane-service.test.ts src/lib/ai/agent/execution/text-lane-service.test.ts src/lib/ai/agent/execution/admin-runner-service.test.ts src/lib/ai/agent/execution/media-admin-service.test.ts src/lib/ai/agent/execution/media-job-action-service.test.ts src/lib/ai/agent/read-models/overview-read-model.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Added [20260331103000_persona_compression_state.sql](/Users/neven/Documents/projects/llmbook/supabase/migrations/20260331103000_persona_compression_state.sql) and synced [schema.sql](/Users/neven/Documents/projects/llmbook/supabase/schema.sql) plus [verification.sql](/Users/neven/Documents/projects/llmbook/supabase/verification.sql), so each persona now has durable `compression_state` metadata without adding a separate compressor status table.
- Added shared [memory-compressor-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/memory/memory-compressor-service.ts), which rebuilds the Phase C compression queue with deterministic priority, respects active defer markers only when the memory fingerprint is unchanged, and persists no-op/defer decisions for personas that were evaluated but do not need compression yet.
- Added [src/agents/memory-compressor/runner.ts](/Users/neven/Documents/projects/llmbook/src/agents/memory-compressor/runner.ts), so compression now has a dedicated background runtime entrypoint instead of depending on admin/manual `compress_once` only.
- Updated [admin-runner-service.ts](/Users/neven/Documents/projects/llmbook/src/lib/ai/agent/execution/admin-runner-service.ts) so `compress_once` now reuses the same shared memory-compressor selection/defer contract as the background worker, rather than using a separate naive persona picker.
- Verified the memory-compressor slice with `npm test -- src/lib/ai/agent/memory/memory-compressor-service.test.ts src/lib/ai/agent/memory/memory-admin-service.test.ts src/lib/ai/agent/execution/admin-runner-service.test.ts src/lib/ai/agent/runtime-state-service.test.ts` plus a clean `npm exec tsc -- --noEmit --pretty false --incremental false`.
- Current remaining runtime backlog is now concentrated on PM acceptance/sign-off and any tiny follow-up fixes it surfaces; the dedicated background memory-compressor entrypoint and Phase C idle-maintenance model are now live.
- Tried targeted ESLint on the new files, but repo linting is currently blocked by a pre-existing `eslint-config-next` / `eslint-plugin-react` runtime error (`react/display-name` loader failure), so lint could not be used as a clean signal for this slice.
