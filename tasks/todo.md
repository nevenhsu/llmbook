# Tasks

## Active

- No active tasks.

## Current References

- LLM-flow docs target folder: `docs/ai-agent/llm-flows`

## Review

- Fixed repeated `persona_core` quality-repair truncation as a flow-design issue, not a global budget issue. The repair loop now gets one final `quality-repair-4` rescue only after repeated `finishReason: length`, and that prompt omits bulky previous parsed JSON, asks for the shortest valid schema object, keeps arrays/strings minimal, and prioritizes closing every object/array. Verification passed with focused persona-generation/API tests (3 files / 30 tests), focused control-panel UI diagnostics tests (4 files / 4 tests), `npm run test:llm-flows` (20 files / 138 tests), `npm run typecheck`, and `git diff --check`.
- Expanded browser diagnostics for persona-generation quality failures to include top-level API error fields, not just nested attempt details. The modal now shows `code`, `stageName`, `issues`, and `details`, so cases like the Lena Hartfield seed repair can expose whether the real rejection is semantic quality, parser schema, or provider output. Verification passed with focused UI/persona-generation tests (7 files / 33 tests), `npm run test:llm-flows` (20 files / 137 tests), `npm run typecheck`, and `git diff --check`.
- Checked the repeated `seed quality repair returned invalid JSON` browser report with the TessMosaic payload. That visible JSON parses successfully, so the flow bug was diagnostic loss/misclassification rather than that shown shape being invalid. Quality-repair parse failures now preserve the exact parser/schema reason as `details.parseError`, and the TessMosaic payload is covered by a parser regression. Verification passed with focused persona-generation/API tests (3 files / 29 tests), `npm run test:llm-flows` (20 files / 137 tests), `npm run typecheck`, and `git diff --check`.
- Fixed the browser-side diagnostic gap for persona generation failures. The API already returned `details` such as `attemptStage`, `finishReason`, provider/model, and `hadText`, but the control-plane modal dropped those details and only showed `result`, which made a truncated or wrapped attempt look like valid JSON in the browser. The hook now preserves error details, and the Persona Generation modal renders an `Attempt Details` block next to `LLM Response`. Verification passed with focused UI/persona-generation tests (8 files / 34 tests), `npm run test:llm-flows` (20 files / 135 tests), `npm run typecheck`, and `git diff --check`.
- Checked the reported `seed` quality-repair invalid JSON case. The pasted seed repair payload parses successfully with `parsePersonaSeedOutput()`, so the error was not caused by the shown schema shape. Added regression coverage for that exact author-reference seed payload and added a seed-specific quality-repair prompt test confirming the first seed repair now includes compact-output guidance. Verification passed with focused persona-generation tests (4 files / 30 tests), `npm run test:llm-flows` (20 files / 135 tests), `npm run typecheck`, and `git diff --check`.
- Fixed persona_core quality-repair invalid JSON from overlong repair output. The issue was partly budget pressure, but the root cause was that the first quality-repair prompt replayed the full previous JSON without compactness constraints, so the model could produce rich prose and fail to close the object before truncation. Added a focused regression test, made first quality repairs compact by default, limited persona_core repair arrays/strings, and raised `qualityRepairCap` from 1800 to 2400 as headroom. Verification passed with focused persona-generation tests (4 files / 28 tests), `npm run test:llm-flows` (20 files / 133 tests), `npm run typecheck`, and `git diff --check`.
- Fixed persona-generation prompt ambiguity that let models emit prose strings for structured fields like `persona_core.values.value_hierarchy`. Added a failing prompt-template test first, then changed the shared stage contract to spell nested leaf types explicitly, including `value_hierarchy: Array<{ value: string; priority: number }>` and `string[]` fields. The `/api/admin/ai/persona-generation/preview` runtime now imports the same prompt constants as the View Prompt template to reduce drift. Verification passed with focused persona-generation tests (4 files / 27 tests), `npm run test:llm-flows` (20 files / 132 tests), `npm run typecheck`, and `git diff --check`.
- Checked `/admin/ai/control-plane` Persona tab → Generate Persona card → View Prompt → Prompt Assembly modal against the actual persona-generation flow. Result: the modal is a mirrored static template preview, not the same exact prompt object built by the runtime flow. It matches the two-stage structure, most shared blocks, stage contracts, and output constraints, but it differs because `admin_extra_prompt` is replaced with a placeholder, `seed` stage goal text is not identical, and the modal cannot include runtime `persona_core` carry-forward context from the generated seed stage. The actual run does return its own `assembledPrompt` after generation, built from `stagePromptRecords`.
- Checked persona generate flow LLM prompts for explicit JSON output formatting. Runtime `seed` and `persona_core` prompts define required key/type shapes in `stage_contract`, pair them with `[output_constraints]` requiring strict JSON/no extra keys, and deterministic parsers reject wrapped/fenced JSON, missing/extra keys, invalid enum status, retired aliases, and extra audit keys. Verification passed with `npx vitest run src/lib/ai/admin/persona-generation-contract.test.ts src/lib/ai/admin/persona-generation-prompt-template.test.ts` (2 files / 14 tests).
- Broadened the dev-server terminal filter so all `GET /api/notifications` request logs are hidden, including query strings and subpaths, while preserving non-GET notification logs and other API logs. Verification passed with `bash -n scripts/run-next.sh`, an AWK sample filter check, `npm run dev -- --help`, and `git diff --check`.
- Updated `/admin/ai/control-plane` Models card so the `Model Errors` panel only includes active persisted models. Disabled/inactive model rows can still show their status in the table, but stale inactive errors are hidden from the error summary. Verification passed with focused UI tests (`2` tests) and `npm run typecheck`.
- Updated text model tests to use `Reply with exactly one word: pong` with `maxOutputTokens: 32` and to fail when the sampled text is empty instead of returning `artifact.text: ""`. Verification passed with focused model/LLM tests (`23` tests) and `npm run typecheck`.
- Added `artifact.text` for successful text model tests, so `/api/admin/ai/models/[id]/test` can return the sampled LLM response instead of `artifact: null` for text models. Verification passed with focused model/LLM tests (`22` tests) and `npm run typecheck`.
- Fixed `/api/admin/ai/models/[id]/test` using manual terminal mode when `AI_AGENT_MANUAL_LLM=true`. Root cause: `invokeLLM()` globally diverted all calls to `dispatchManualLlmQueue`; model tests now pass `manualMode: "never"` so they call real provider APIs while runtime LLM responses keep the default manual behavior. Verification passed with focused LLM tests (`21` tests) and `npm run typecheck`.
- Set the default text runtime fallback to `deepseek/deepseek-v4-flash` in code and env templates. Verification passed with `npx vitest run src/lib/ai/llm/default-model-config.test.ts` (`6` tests) and `npm run typecheck`.
- Added DeepSeek runtime support with `@ai-sdk/deepseek`, `DEEPSEEK_API_KEY` env fallback, default model config for `deepseek-v4-flash`, registry inclusion, admin provider/model catalog entries, and a `DeepSeek-V4-Flash` display label. Verification passed with focused DeepSeek LLM tests (`14` tests) and `npm run typecheck`.
- Fixed the Interaction Preview migration for `/admin/ai/control-plane`: post/comment/reply previews now run through the same registered flow modules used by `AiAgentPersonaTaskGenerator` without importing the generator into admin preview code. The preview response now carries flow diagnostics, the existing modal surfaces final status, terminal stage, attempts, and gate details, and reply is accepted through the route/UI path. Also restored the mock preview page's seeded task context after the Gemini change had blanked it. Verification passed with `npm run typecheck`, `npm run test:llm-flows` (20 files / 131 tests), and the focused Interaction Preview suite (5 files / 17 tests).
- Cleaned up the Interaction Preview task-type boundary: admin preview explicitly rejects internal post-stage task types such as `post_body`, the route error now lists `reply`, and the service fallback no longer carries old `post`/`post_body` rendering branches. User-facing `post` remains the full staged `post_plan -> post_body` flow. Verification passed with `npm run typecheck`, `npm run test:llm-flows` (20 files / 131 tests), and focused Interaction Preview tests (5 files / 18 tests).
- Verified `/admin/ai/control-plane` → `Interaction Preview` against the current LLM interaction-flow framework. Conclusion: partially aligned, not fully. The tab is not a prompt-only stub and it does use the shared `previewPersonaInteraction()` / `AiAgentPersonaInteractionService` / `runPersonaInteractionStage()` path with selected model routing, persisted persona core, compact prompt assembly, render parsing, and zero admin provider retries. However, the tab calls only `stagePurpose: "main"` and returns `auditDiagnostics: null`, so it bypasses the newer `post/comment/reply` flow modules that perform schema repair, compact semantic audit, quality repair, and flow diagnostics. The route/UI also omits `reply` from the selectable/accepted task types despite the spec listing it. Verification passed with `npm run test:llm-flows` (20 files / 131 tests) plus focused interaction preview tests (3 files / 11 tests).
- Verified `/admin/ai/control-plane` → `Generate Persona` against the current LLM-flow persona-generation contract. The button path uses `POST /api/admin/ai/persona-generation/preview`, which runs the dedicated two-stage `seed -> persona_core` staged JSON flow with parse repair, deterministic quality checks, semantic audit, quality repair, compact carry-forward context, and admin preview retries capped at `0`. `npm run test:llm-flows` passed 20 files / 131 tests. Minor caveat: the pre-run `View Prompt` modal uses a mirrored static prompt-template preview, so it is currently aligned but remains a possible drift point from the runtime service.
- Completed the remaining LLM-flow hardening pass: strict persona-generation parser contracts, fail-closed persona semantic audits, no generate-persona memory payload forwarding, semantic `post_plan` audit/repair, runtime/operator flow-failure diagnostics, and consistent `[output_constraints]` JSON key/type shapes for the previously missing prompts.
- Final verification passed with `npm run verify`: typecheck passed, lint passed with the existing 9 warnings, and `test:llm-flows` passed 20 files / 131 tests.
- Moved completed LLM-flow plan/reference documents into `docs/ai-agent/llm-flows` and updated repo references to the new docs location.
- Tidied `docs/ai-agent/llm-flows`: added a docs index, archived completed implementation plans, renamed current reference docs, removed task-execution sections from current references, and verified stale path checks.
